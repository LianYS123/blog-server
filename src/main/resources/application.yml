
mybatis:
  mapper-locations: classpath:**/*Mapper.xml
  type-aliases-package: fun.lianys.blogserver.model.entity # 包名，不是类名
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog
    username: root
    password: tb1766318380
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
  devtools:
    livereload:
      enabled: true
  servlet:
    multipart:
      max-file-size: 10MB
swagger:
  enable: true
  application-name: ${spring.application.name}
  application-version: 1.0
  application-description: liuli api
  try-host: http://localhost:${server.port}
server:
  port: 8090
  servlet:
    context-path: /api
logging:
  file:
    name: blog.log

# 自定义的一些配置
file.upload.path: files/
cos.secretId: AKIDymKbcMq2yd18yXpqa4HTVMxHK6bpmSr0
cos.secretKey: sUpthEUTBbkcCVXImzvay4tRiiRs2wZv
jwt.secretKey: amF2YXguY3J5cHRvLnNwZWMuU2VjcmV0S2V5U3BlY0BmYTc3YzE0ZQ==
